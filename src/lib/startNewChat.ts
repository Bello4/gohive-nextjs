async function startNewChat(
  userName: string,
  userEmail: string,
  userPhone: string,
  chatbotId: number,
) {
  try {
  } catch (error) {
    console.log("Error starting new chat session:", error);
  }
}
